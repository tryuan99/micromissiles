syntax = "proto3";

package swarm;

import "simulation/swarm/proto/state.proto";

// Target swarm configuration.
message TargetSwarmConfig {
  // State mean.
  State mean = 1;

  // State standard deviation.
  State standard_deviation = 2;

  // Kill probability.
  float kill_probability = 3;
}
