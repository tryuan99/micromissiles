syntax = "proto3";

package swarm;

import "simulation/swarm/proto/target_config.proto";

// Target swarm configuration.
message TargetSwarmConfig {
  // Number of targets.
  uint32 num_targets = 1;

  // Target configuration.
  TargetConfig target_config = 2;
}
